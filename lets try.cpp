#include <iostream>
using namespace std;
float a;
int main()
{
    cout << "hi saif how are u please we are looking for u to work with us pls enter ur number" << a << endl;
    cin >> a ;
    cout << "thank u for replying" << a << true << endl;


        {
            if
            bool a=true,f=false,v=false,h=false,q=false;

             cout << "sorry its wrong info pls try again later" << v << endl;
        cin >> v;
    if (h)
        cout << "pls quit " << h << endl;
        cin >> h;
    if (q)
        cout << "its wrong try again later" << q << endl;
        if (f)
        cout << "sorry wrong info" << f << endl;
        cin >> f;

        }



    return 0;
}
